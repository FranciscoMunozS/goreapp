!!!
%html
  %head
    %title Gobierno Regional de Los Ríos - Boletas de Garantías
    = csrf_meta_tags
    = stylesheet_link_tag    'application', media: 'all'
    = javascript_include_tag 'application'
  %body
    - if user_signed_in?
      = render 'assets/nav'
    - flash.each do |name, msg|
      = content_tag(:div, msg, class: "alert ui message")
    .container
      = yield
